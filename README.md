This replication package requires `uv`.

The instructions to generate all intermediate files are reported in `Makefile`.

Usage:

```
git submodule init
git submodule update
make experiments
```

Benchmark times will be reported in `experiments.log`.
